[Mouse]
; Experimental A/B switch. Default OFF.
; 0 = pure observation, vanilla AB9830 output is preserved.
; 1 = on the verified mouse-look path only, after a fresh non-zero DirectInput
;     mouse read on the same thread, restore pre-AB9830 X/Y only when |pre| > 1.
EnableMouseAB9830Bypass=0
